- Gọi hàm và truyền tham số trong function expression và lambda function(sử dụng =>).
- Tìm hiểu về DOM,vẽ cây DOM, các thẻ trong HTML, bao gồm các thẻ <div>, <a>, <title>, <h1>,..
- Tìm hiểu về selector 
● XPath
● CSS selector
● Playwright selector
- XPath : tương đối và tuyệt đối, thường sẽ dùng tương đối. 
- Playwright 